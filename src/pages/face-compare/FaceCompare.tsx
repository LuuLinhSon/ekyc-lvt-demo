import React from 'react';
import './FaceCompare.scss';

const FaceCompare: React.FC<any> = (props) => {
  return (
      <div className="container">
        Face Compare
      </div>
  );
};

export default FaceCompare;
